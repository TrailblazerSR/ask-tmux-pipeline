interface:
  display_name: "Ask Tmux Claude"
  short_description: "Ask Claude through the local reusable tmux consultant session"
  default_prompt: "Use ask-tmux-claude only from the current machine for explicit Claude review/comment/suggest or ask-tmux-claude requests. Do not call remote wrappers; cross-machine access is only for repo/install alignment."
