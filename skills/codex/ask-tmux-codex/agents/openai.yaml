interface:
  display_name: "Ask Tmux Codex"
  short_description: "Ask Codex through the local reusable tmux consultant session"
  default_prompt: "Use ask-tmux-codex only when the user explicitly names ask-tmux-codex or asks for an ask-tmux Codex reviewer/consultant. Do not use for generic 'call another Codex session' requests, including on HPC; use only local wrappers."
