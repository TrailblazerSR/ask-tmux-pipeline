#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
runner="$script_dir/ask-tmux-pipeline"
if [[ ! -x "$runner" ]]; then
  runner="$(command -v ask-tmux-pipeline 2>/dev/null || printf '/home/h3031/bin/ask-tmux-pipeline')"
fi

cmd="${1:-}"
if [[ -z "$cmd" ]]; then
  exec "$runner" --help
fi

case "$cmd" in
  start|answer|review|status|resume|final-context|doctor|-h|--help|help)
    shift || true
    exec "$runner" "$cmd" --provider claude "$@"
    ;;
  --*)
    exec "$runner" start --provider claude "$@"
    ;;
  *)
    exec "$runner" start --provider claude "$@"
    ;;
esac
